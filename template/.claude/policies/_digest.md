# Policy Digest — Global (.claude/policies/)

> Auto-generated by `scripts/build-policy-digest.sh`. Do not edit by hand.
> Source: `.claude/policies`
> Policies: 171 (95 hard / 76 soft)

## Hard-enforcement (95)

- [hard] **articles-blog-first**: Any content with `type: article` in draft frontmatter or assessed as "Article" scope in `/contentidea` must follow the blog-first pipeline:
- [hard] **blog-post-x-draft**: When publishing a new blog article (writing MDX to `repos/private/personal-website/src/content/blog/`), always draft an X post to share the article before de...
- [hard] **company-archive-cleanup**: When archiving or deleting a company, always:
- [hard] **company-context-verify**: Before any company-scoped operation:
- [hard] **credential-access-protocol**: Before accessing ANY company credentials (`companies/{co}/settings/`):
- [hard] **curious-minds-is-book**: `knowledge/public/curious-minds/` is a book (concept reference), NOT company knowledge docs. Skip it during:
- [hard] **deconflict-postbridge-schedule**: Before scheduling a batch of posts via Post-Bridge:
- [hard] **eas-env-not-dotenv**: EAS remote builds do NOT read `.env.local` files. Any `EXPO_PUBLIC_*` env vars needed in the build must be added to either:
- [hard] **email-humanize**: Before sending or drafting any outbound email (cold outreach, replies, follow-ups), run the content-brand humanize pass:
- [hard] **email-send-safety**: 1. NEVER send an email without explicit user confirmation of the sending account. If the user says "send from {Product}" and that account fails, STOP. Do not...
- [hard] **expo-securestore-keys**: `expo-secure-store` only allows keys matching `/^[\w.-]+$/` — letters, digits, underscores, dots, hyphens. Colons are NOT allowed. Use dots as namespace se...
- [hard] **feature-flag-first**: 1. Any new feature that affects ETL Lambdas, CubeJS cubes, or frontend dashboard pages in the {PRODUCT} repo MUST be gated behind `brand_permissions` with a ...
- [hard] **git-filter-repo-case-variants**: When using `git filter-repo --replace-text`, add explicit replacement rules for EVERY case variant of each term (lowercase, Capitalized, UPPERCASE). The tool...
- [hard] **gmail-mcp-config**: 1. The ONLY gmail MCP is `advanced-gmail-mcp` at `repos/public/advanced-gmail-mcp/`. Server name in `.mcp.json` is `gmail`.
- [hard] **hq-announce-before-irreversible**: 1. Before executing any irreversible action, state what you are about to do, which account/project/environment it targets, and ask for explicit confirmation....
- [hard] **hq-bash-set-e-status-returns**: When a bash function intentionally returns non-zero exit codes as status signals (e.g., `handle_failure` returning 2=skip, 3=pause), NEVER call it directly u...
- [hard] **hq-bugfix-requires-tests**: When fixing a bug or broken behavior, always add tests or E2E coverage that would catch the regression if it recurred. If unsure about test type or scope (un...
- [hard] **hq-cmd-publish-kit-rsync-deref**: ALWAYS use `rsync -avL` (with `-L` flag) when copying `knowledge/public/` to `repos/public/hq/template/`. Without `-L`, rsync copies symlinks as symlinks, wh...
- [hard] **hq-company-scoped-archives**: Archived projects go to `companies/{co}/projects/_archive/` (NOT central `projects/_archive/`). All company data lives under its company dir. Active projects...
- [hard] **hq-data-collection-isolation**: NEVER pull high-volume monitoring data (metrics, health checks, dashboards) inline in the parent session — each tick burns 50K+ tokens of tool results.
- [hard] **hq-docker-build-platform-amd64**: ALWAYS use `docker buildx build --platform linux/amd64` when building images for ECS/EC2 deployment. Never use plain `docker build` on Apple Silicon Macs for...
- [hard] **hq-docker-in-docker-path-translation**: When a container spawns sibling containers via the mounted Docker socket (`/var/run/docker.sock`), bind-mount source paths must use the outer host filesystem...
- [hard] **hq-email-ascii-subjects**: NEVER use special characters (em dash, curly quotes, Unicode punctuation) in email subject lines — they encode as garbled text. Plain ASCII only: hyphens n...
- [hard] **hq-fix-root-cause-not-symptoms**: NEVER fix a bug by hiding or masking the error. When investigating a failure, always trace to the root cause and fix the actual problem — not just improve ...
- [hard] **hq-git-branch-verify**: ALWAYS run `git branch --show-current` before committing to any repo. Never assume the current branch — inherited cwd or installs can silently land you on ...
- [hard] **hq-gitignore-before-first-commit**: ALWAYS create `.gitignore` (with `node_modules/`, `.next/`, `.vercel/`, build artifacts) BEFORE running `git init && git add -A && git commit`. If build arti...
- [hard] **hq-glob-scoped-path**: ALWAYS pass `path:` to Glob scoped to a subdirectory (e.g. `projects/`, `workers/`). Glob from HQ root times out (`.ignore` doesn't protect it). Grep from HQ...
- [hard] **hq-image-context-isolation**: The parent session must never accumulate more than ~10 images in its context window. When reading, verifying, or analyzing image files, delegate to a sub-agent.
- [hard] **hq-linear-api-no-bearer**: ALWAYS use `Authorization: <api_key>` (plain key, no prefix) when authenticating with the Linear GraphQL API. NEVER use `Authorization: Bearer <api_key>` —...
- [hard] **hq-mcp-absolute-paths**: ALWAYS use absolute paths (e.g. `/opt/homebrew/bin/npx`, `/opt/homebrew/bin/node`) for `command` fields in `.mcp.json` stdio-type servers. Bare `npx` or `nod...
- [hard] **hq-never-echo-tokens-stdout**: NEVER print raw API keys, tokens, or secrets to stdout in CLI setup/config commands. Use `<your-api-key>` or `<paste-your-key>` placeholders instead. CLI set...
- [hard] **hq-never-swallow-errors**: 1. NEVER write a catch block that returns null, undefined, empty array, or a success status without logging the error. Every catch block must either re-throw...
- [hard] **hq-no-chrome-mcp-slack-fallback**: When the `slack` MCP `send_message` tool is unavailable, NEVER fall back to Claude in Chrome (`mcp__Claude_in_Chrome__*`) to post Slack messages.
- [hard] **hq-no-glob-discovery**: Never use Glob to find `prd.json`, `worker.yaml`, or discover project/company/worker directories. This is hook-enforced — Glob calls with these patterns ar...
- [hard] **hq-no-grep-discovery**: Never use Grep to find `prd.json`, `worker.yaml`, or discover project/company/worker directories. This is hook-enforced — Grep calls with these patterns ar...
- [hard] **hq-no-production-testing**: 1. NEVER use production API endpoints, credentials, or accounts for testing, debugging, or development. Always ask which environment to target. If no staging...
- [hard] **hq-no-supabase-deletion**: NEVER delete Supabase projects without confirming with user first. Projects that appear unused may still be active production databases. Always ask before de...
- [hard] **hq-no-test-shortcuts**: Always fix tests properly — never skip flaky tests, never add `test.skip` as a workaround, never create false positives. When a test fails, investigate roo...
- [hard] **hq-no-worktree-for-repo-work**: NEVER create a git worktree or new branch in HQ when starting work on a project repo ({PRODUCT}, {company}-gtm-hq, etc.). HQ must stay on `main` at all times...
- [hard] **hq-og-images-required**: Every site deployed to Vercel (or any public hosting) MUST have Open Graph images configured before the first production deploy. This includes:
- [hard] **hq-orchestrator-lockfile-sync**: After all stories complete in run-project.sh, run `npm install` (or equivalent package manager install) in the project repo and commit the updated lockfile b...
- [hard] **hq-paper-mcp-sequential-agents**: When spawning sub-agents for Paper MCP design work, agents MUST run sequentially — never in parallel. Paper operates on a single shared canvas; two agents ...
- [hard] **hq-post-bridge-api-field-names**: When calling `POST https://api.post-bridge.com/v1/posts` directly (not via SDK), use these exact field names:
- [hard] **hq-post-parallel-build-verify**: 1. After parallel agents complete, ALWAYS run the project's build and typecheck before reporting success. This is not optional — parallel agents cannot see...
- [hard] **hq-pr-single-concern**: 1. Every PR must address exactly one concern. A concern is: one bug fix, one feature, one refactor, one infrastructure change, or one dependency update. If y...
- [hard] **hq-prd-no-implement**: When `/prd` is invoked, the ONLY outputs are:
- [hard] **hq-pre-deploy-domain-check**: Before ANY Vercel deploy:
- [hard] **hq-pull-before-work**: ALWAYS run `git pull` (or `git fetch && git merge`) on the active branch before making any changes to a repo. If the repo is significantly behind origin (50+...
- [hard] **hq-regression-gate-baseline**: Regression gates in `run-project.sh` MUST compare error counts against a baseBranch baseline. Only fail when errors increase beyond the baseline — pre-exis...
- [hard] **hq-run-project-repo-bootstrap**: Before launching `run-project.sh`, verify the PRD's `metadata.repoPath` exists as a git repo with at least one commit. The script uses git worktrees for bran...
- [hard] **hq-scheduled-task-reference-pattern**: When creating or updating a scheduled task via the scheduled-tasks MCP tools, the system overwrites `SKILL.md` with the `prompt` parameter content. Any detai...
- [hard] **hq-social-draft-content-extraction**: Social draft files use this format:
- [hard] **hq-subagent-no-mcp**: Sub-agents spawned via Task() don't inherit MCP server connections. Workers needing external tools (Codex, etc.) must use CLI via Bash, not MCP tools declare...
- [hard] **hq-swarm-pr-branch**: After `run-project --swarm` completes, all story commits are cherry-picked onto local `main` (the working tree), NOT the PRD's `branchName`. To create a PR:
- [hard] **hq-telegram-single-poller**: Telegram's Bot API allows only ONE active `getUpdates` (long-polling) connection per bot token. A second poller causes 409 Conflict errors and crashes the ne...
- [hard] **hq-use-neon-not-vercel-postgres**: ALWAYS use `@neondatabase/serverless` instead of `@vercel/postgres` for Vercel-hosted Postgres. `@vercel/postgres` is deprecated — Vercel migrated all Post...
- [hard] **hq-vercel-custom-domain-safety**: NEVER deploy to a production custom domain (e.g. app.example.com, store.example.com) without explicit user confirmation. "Deploy to a temporary Vercel site" ...
- [hard] **hq-vercel-env-no-newlines**: When piping values to `vercel env add`, ALWAYS use `printf` (no trailing newline) — NOT `echo`. `echo` appends `\n` to the value, causing API calls with th...
- [hard] **hq-vercel-env-no-trailing-newline**: ALWAYS use `printf '%s' "$VALUE" | vercel env add NAME production` instead of `echo "$VALUE" | vercel env add`. `echo` appends a trailing newline which Verce...
- [hard] **hq-vercel-framework-detection**: If Vercel project has `framework: null`, production builds deploy but serve 404 on all routes (even though build succeeds). Two fix methods:
- [hard] **hq-vercel-pnpm-version-pin**: ALWAYS add `"packageManager": "pnpm@X.Y.Z"` to `package.json` for any pnpm project deployed to Vercel. Without this field, Vercel auto-selects pnpm version "...
- [hard] **hq-vercel-project-id-collision**: Before any `vercel deploy` or `vercel link`, check `settings/deploy-registry.yaml` for the target project ID. If two repos share the same `project_id`, deplo...
- [hard] **linear-scan-check-existing-prds**: Before recommending a new PRD from a Linear scan, always check `companies/{company}/projects/` for existing PRDs that cover the same Linear issues. Use `ls c...
- [hard] **mcp-process-cleanup**: MCP servers spawned via stdio (npx/tsx) leak as orphaned processes when Claude sessions end. The `cleanup-mcp-processes` Stop hook kills these on session exi...
- [hard] **mcp-transport-detection**: Before adding a new MCP server to `.mcp.json`, test the transport protocol. Not all HTTP MCP servers accept plain `"type": "http"` (JSON POST). Servers using...
- [hard] **model-routing-opus-only**: All Claude work runs on Opus 4.6. Never use Sonnet.
- [hard] **no-hq-remote-push**: NEVER push HQ data to any remote repository. HQ is local-only. The `origin` remote (`indigoai-us/hq`) is used only for PULLING upstream updates, not for push...
- [hard] **no-slack-channel-posts-without-permission**: NEVER post messages to Slack channels without explicit user permission. Always ask first before sending to any channel. DMs to specific people the user reque...
- [hard] **no-threaded-posts**: Never generate, suggest, queue, or post content with type "thread" (multi-part X threads like 1/5, 2/5, etc.).
- [hard] **post-bridge-media-upload**: When scheduling posts in Post-Bridge with images:
- [hard] **post-bridge-media-workflow**: When scheduling posts in Post-Bridge, images cannot be added inline. Use the 2-step flow:
- [hard] **post-bridge-unicode-payload**: NEVER shell-expand JSON payloads containing unicode characters (em dashes —, curly quotes, etc.) for Post-Bridge API calls. The shell's `$(...)` expansion ...
- [hard] **prd-json-schema**: When creating `prd.json` files:
- [hard] **prd-json-validation-post-task**: After any sub-agent writes to prd.json (setting `passes`, adding `notes`, updating `files`), validate the JSON is parseable before proceeding to the next sto...
- [hard] **prd-no-execute**: 1. After `/prd` creates project files (`prd.json` + `README.md`), run `/handoff` and end the session
- [hard] **prd-userstories-key**: PRDs MUST use `"userStories"` as the array key for stories (not `"stories"`). Every story object MUST include `"passes": false`. `run-project.sh` validates b...
- [hard] **publish-kit-no-subagents-for-scrub**: During `/publish-kit`, NEVER delegate file scrubbing (PII replacement, denylist application) to sub-agents. Sub-agents hit Write/Bash/Edit permission denials...
- [hard] **publish-kit-{company}-sed-ordering**: The scrub function uses a two-phase approach driven by `.claude/scrub-denylist.yaml`:
- [hard] **repo-run-coordination**: When a `/run-project` owns a repo, no other Claude session may edit files inside
- [hard] **run-project-local-keyword**: Never use `local` keyword in the top-level swarm or sequential mode loop bodies of `run-project.sh`. These loops are NOT inside functions — `local` only wo...
- [hard] **run-project-name-matches-dir**: When invoking `scripts/run-project.sh` or `/run-project`, the project argument must exactly match the directory name under `companies/*/projects/{name}/`. Do...
- [hard] **run-project-no-inline**: When user invokes `/run-project` or asks to execute a project with a `prd.json`, ALWAYS launch `scripts/run-project.sh` as the orchestrator. NEVER implement ...
- [hard] **run-project-no-permissions-required**: ALWAYS pass `--no-permissions` when launching `run-project.sh` for an unattended (non-interactive) orchestrator run.
- [hard] **scrub-hook-no-denylist-in-template**: NEVER copy `.claude/scrub-denylist.yaml` to the template dir (`repos/public/hq/template/`) during `/publish-kit`. The denylist contains real private terms in...
- [hard] **scrub-hook-skip-lockfiles**: The pre-commit scrub-check hook MUST skip `package-lock.json` and `bun.lockb` files. SHA-512 integrity hashes in these files contain random base64 strings th...
- [hard] **shopify-block-order**: In Shopify JSON templates, the key for ordering blocks is `block_order` (singular), NOT `blocks_order`.
- [hard] **shopify-client-credentials**: Shopify no longer issues permanent `shpat_*` Admin API access tokens. Custom apps use Client Credentials Grant (OAuth 2.0):
- [hard] **slack-mcp-private-channels**: ALWAYS use the custom slack-mcp (`repos/public/slack-mcp/`), NEVER the official Slack marketplace plugin. The official plugin uses OAuth bot tokens that cann...
- [hard] **tauri-event-payload-encoding**: When receiving data from Tauri's JS event system (`app.listen()` + `window.__TAURI__.event.emit()`), the payload arrives double-JSON-encoded. A JSON array `[...
- [hard] **tauri-plugin-setup-async**: In Tauri v2 plugin `setup` callbacks, use `tauri::async_runtime::spawn()` instead of `tokio::spawn()`. The setup closure runs before the Tokio runtime is acc...
- [hard] **vercel-cross-team-domains**: Vercel does NOT allow adding a domain registered on Team A to a project on Team B (returns 403). When a domain and its destination project are on different V...
- [hard] **vercel-env-no-echo**: When adding Vercel environment variables via CLI pipe, ALWAYS use `printf` instead of `echo`:
- [hard] **vercel-git-disconnect-cwd**: NEVER run `vercel git disconnect` or `vercel link` from HQ root or any directory other than the target repo. These commands operate on the `.vercel/project.j...
- [hard] **vercel-monorepo-root-directory**: When a Next.js app lives in a subdirectory (e.g. `site/`), `vercel deploy --yes` from the repo root creates a NEW project that fails to detect the framework ...

## Soft-enforcement (76)

- [soft] **agent-browser-react-false-positives**: When running E2E tests with agent-browser against data-dependent pages:
- [soft] **anthropic-pricing-verify**: Always web-search current Anthropic pricing before hardcoding cost constants. Pricing changes frequently — stale rates can inflate cost estimates by 3x or ...
- [soft] **anthropic-sdk-vercel-bundling**: When deploying a Next.js app that uses `@anthropic-ai/sdk` to Vercel, add it to `serverExternalPackages` in `next.config.ts`:
- [soft] **ascii-art-character-verify**: When manually composing ANSI Shadow block art, ALWAYS verify each character against the skill's character map before screenshotting. Characters T (`╚══...
- [soft] **bulk-sed-exception-ordering**: When doing a bulk `find + sed` sweep across many files with specific exceptions, ALWAYS:
- [soft] **bun-overrides-devtool-conflict**: Bun's `overrides` in package.json apply project-wide — including to devtools' internal dependencies. When a devtool (e.g. knip) requires a different versio...
- [soft] **chrome-mcp-multi-page-audit**: When auditing multiple pages via Chrome MCP:
- [soft] **chunked-reads-large-files**: For files over 500 LOC, use the `offset` and `limit` parameters to read in sequential chunks. Never assume a single Read captured the complete file — the R...
- [soft] **cio-browser-navigation**: When navigating Customer.io via agent-browser:
- [soft] **clerk-vercel-edge-removal**: Removing `@clerk/nextjs` from code + env vars is NOT enough to remove Clerk auth from a Vercel project. Clerk injects middleware at the Vercel edge infrastru...
- [soft] **clipboard-file-protocol**: `navigator.clipboard.writeText()` requires a secure context (HTTPS). Pages opened via `file://` protocol will always reject the Clipboard API. When building ...
- [soft] **dual-codex-review-pattern**: For high-value PRs (10+ stories, security-sensitive code, new feature areas), run both a Claude-based review and `codex review --base {branch}` (gpt-5.4). Th...
- [soft] **dual-repo-prd-routing**: When a PRD spans two repos (`repoPath` + `secondaryRepoPath`), add a `REPO:` prefix in each story's `notes` field directing the sub-agent to the correct repo...
- [soft] **figma-icon-export**: Figma MCP screenshots include canvas background and cannot be saved as files. For app icon work, have the user export the icon from Figma as a high-res PNG, ...
- [soft] **ggshield-global-hook-workaround**: When `git commit` fails because a global ggshield pre-commit hook exists (`~/Library/Application Support/ggshield/git-hooks/pre-commit`) but the `ggshield` b...
- [soft] **gmail-token-refresh**: Before sending emails via Gmail MCP, test token validity with a lightweight call first (e.g., `mcp__gmail__list_emails` with limit 1). If it fails with `inva...
- [soft] **google-fonts-satori**: When loading fonts for `next/og` (Satori), never hardcode Google Fonts gstatic URLs — they change with font versions (e.g., v22 → v24). Instead, fetch th...
- [soft] **gstack-prefix-mapping**: gstack skills are installed with a `g-` prefix. Their SKILL.md prose references sibling
- [soft] **guest-auth-wasGuestRef**: In {company}'s `AuthContext.tsx`, `wasGuestRef.current` must be set to `true` in ANY code path that enters guest mode. Without it, `onAuthStateChange` won't ...
- [soft] **hook-macos-case-paths**: When writing bash hooks that compare file paths (e.g. checking if a path is inside `repos/`), always use case-insensitive matching or pattern-based checks (e...
- [soft] **hq-alert-baseline-calibration**: 1. Before setting alert thresholds, document the expected baseline behavior including known patterns that should NOT trigger alerts. If 51% of a certain even...
- [soft] **hq-cmd-run-project-pid-tracking**: When monitoring `run-project.sh` execution, NEVER rely solely on PID liveness from check-in status. The orchestrator tracks the shell wrapper PID (`bash -c "...
- [soft] **hq-cmd-run-project-process-cleanup**: Before launching `run-project.sh` (especially with `--resume`), always check for existing orchestrator and sub-agent processes:
- [soft] **hq-cmd-run-project-swarm-orphan-recovery**: When `run-project.sh` swarm mode reports "passes not set" despite a sub-agent exiting with code 0, do NOT assume the work was lost. The sub-agent's commits m...
- [soft] **hq-codex-skill-dual-format**: Commands (`.claude/commands/*.md`) and skills (`.claude/skills/*/SKILL.md`) are different formats with different discovery mechanisms. Codex discovers skills...
- [soft] **hq-compute-checksums-path**: The checksum script for HQ Core Governance is at `template/scripts/compute-checksums.sh` (not `scripts/compute-checksums.sh`). Requires `yq` (`brew install y...
- [soft] **hq-confirm-creative-direction**: 1. Before building any creative deliverable, present the intended direction and get explicit approval. Describe the visual style, color palette, tone, layout...
- [soft] **hq-github-review-thread-resolution**: - GitHub REST API has no endpoint for resolving PR review threads. Use the GraphQL `resolveReviewThread` mutation instead.
- [soft] **hq-gnb-product-imagery-remix**: NEVER generate branded product imagery (cans, bottles, packaging) from text descriptions alone. AI image generators consistently produce wrong brand text, co...
- [soft] **hq-mermaid-diagrams-only**: When creating/editing knowledge files, ALWAYS use ` ```mermaid ` blocks for diagrams — never ASCII art. The {your-app} renders Mermaid as interactive SVG w...
- [soft] **hq-nextjs-clean-types-after-page-delete**: After deleting a Next.js page file, run `rm -rf .next` before `tsc --noEmit` or `pnpm typecheck`. The `.next/types/validator.ts` cache retains references to ...
- [soft] **hq-prefer-agent-browser**: For browser-based QA audits, page-walking, and site testing, use the Vercel agent-browser CLI tool (invoked via `/agent-browser` skill) instead of Claude in ...
- [soft] **hq-run-project-sigkill-retry**: When a `claude -p` subprocess in `run-project.sh` fails with exit code 137 (SIGKILL), do NOT manually intervene. macOS memory pressure is the most common cau...
- [soft] **hq-site-rebuild-asset-audit**: ALWAYS compare total asset count between source site and destination repo when scraping assets for site rebuilds. Do not rely solely on pattern-matching bran...
- [soft] **hq-skill-plugin-pattern**: Integrate external Claude Code skill packages as namespaced plugins using the bridge
- [soft] **hq-slack-channel-{company}-workspace**: The `#hq` Slack channel lives on the {Product} workspace. When posting HQ updates, always specify `workspace: "{company}"`. Full details (channel ID, post ty...
- [soft] **hq-supabase-cli-project-ref-verify**: ALWAYS verify the Supabase CLI-linked project ref matches the `.env.local` URL before using keys from `supabase projects api-keys`. Decode the JWT to check: ...
- [soft] **hq-supabase-storage-bucket-creation**: Before uploading to Supabase Storage via `PUT /storage/v1/object/{bucket}/{path}`, verify the bucket exists. If it returns 404 "Bucket not found", create it ...
- [soft] **hq-swarm-rust-hub-files**: Rust projects have "hub files" that nearly every feature story modifies: `Cargo.toml`, `src/lib.rs`, `src/commands/mod.rs`. When running swarm mode on Tauri/...
- [soft] **hq-tmux-plan-approval-dance**: When `/run-project --tmux` launches a Claude session in tmux, the session requires 3 sequential approvals before execution begins:
- [soft] **hq-vercel-deployment-protection**: 1. `vercel deploy --public` makes source public, NOT bypasses deployment protection (SSO). Vercel preview URLs always require login unless project-level prot...
- [soft] **hq-vercel-domain-team-move**: When purchasing a domain via Vercel/Name.com, it can land in the wrong team/org. Check ownership with `GET /v6/domains/{domain}?teamId={teamId}` across all t...
- [soft] **hq-vercel-env-pull-environment**: ALWAYS check `vercel env ls` to see which environment vars are scoped to before pulling. `vercel env pull` defaults to "development" — production-only vars...
- [soft] **hq-verify-shared-files-after-parallel-agents**: When 3+ parallel agents all edit the same file (e.g., registry.yaml, package.json, INDEX.md), always read the final file after all agents complete and verify...
- [soft] **idb-install**: To install Facebook's `idb` (iOS debug bridge) for iOS simulator control:
- [soft] **nextjs-suspense-dev-fallback**: When a Next.js 16 app with Turbopack dev mode shows RSC pages stuck on their Suspense fallback ("Loading...") despite the server returning 200, do not debug ...
- [soft] **og-image-twitter-cache**: When adding or changing OG images for a site, always deploy and verify the OG image endpoint returns valid PNG before sharing the URL on X/LinkedIn. X caches...
- [soft] **orchestrator-competing-processes**: Before running `--resume` on a project, check for existing `run-project.sh` processes for that project (`ps aux | grep "run-project.*{project}"`). Kill all s...
- [soft] **paginated-api-sort-priority**: When a paginated API serves data that the client filters by status/priority, add server-side `sort` param to return high-priority records first. Client-side ...
- [soft] **paper-flex-column-reorder**: When a Paper artboard uses `display: flex; flex-direction: column` (most page-level artboards do), `write_html insert-children` always appends new nodes at t...
- [soft] **paper-text-width**: When writing HTML via Paper MCP (`write_html`), always set an explicit `width` property on Text nodes (e.g. `width: 260px; text-align: center`). Without expl...
- [soft] **paper-text-wrapping**: Paper MCP text nodes do NOT respect `width` or `max-width` for text wrapping. Text renders as a single unwrapped line regardless of container or node width s...
- [soft] **post-edit-verification**: After completing edits in a repo that has typecheck or lint configured, run the project's verification commands before reporting the task as done:
- [soft] **prd-content-sources**: When creating PRD stories that involve content extraction or analysis of a person's written/spoken work, enumerate ALL available source locations in HQ — n...
- [soft] **prd-files-match-acs-for-swarm**: Before launching `/run-project` with `--swarm`, verify that each story's `files[]` array includes ALL files mentioned in its acceptance criteria. Swarm mode ...
- [soft] **prd-story-sizing**: Every user story in a PRD must have a complexity score computed as:
- [soft] **pre-refactor-hygiene**: Before structural refactors on files >300 LOC, first remove dead code in a separate commit:
- [soft] **preview-clerk-auth**: Preview tools (preview_screenshot, preview_snapshot) render blank for apps using Clerk middleware auth. The headless browser has no Clerk session. Verify via...
- [soft] **preview-tool-node-path**: The preview_start tool's shell environment does not have fnm (or nvm) in PATH. Commands like `npm`, `npx`, and bare `node` are not found.
- [soft] **publish-kit-scope-audit**: When adding a new category of files to HQ (skills, policies, infrastructure configs), check whether it should be added to the publish-kit sync scope. The syn...
- [soft] **qmd-collection-masks**: When creating or updating qmd collections, include all file types that should be searchable — not just `.md`. The `hq` collection uses `/*.{md,json,yaml,ym...
- [soft] **regression-gate-lint-fix**: When a regression gate fails due to lint error increase (e.g. "14 errors, baseline 13, +1 new"), fix the new error directly on the feature branch (checkout, ...
- [soft] **rename-safety-checklist**: When renaming or changing the signature of any function, type, or variable, search separately for all reference categories — a single grep will miss indire...
- [soft] **reread-before-edit-long-sessions**: After 10+ conversation turns, or if context compaction has occurred, re-read any file before editing it. Do not trust cached/remembered file contents — com...
- [soft] **session-data-for-product-accuracy**: Before writing user-facing HQ guides or cheatsheets, analyze actual session data in `workspace/threads/` and `workspace/learnings/.observe-patterns*.json` to...
- [soft] **subagent-creative-dedup**: When using parallel sub-agents to generate creative content (social posts, marketing copy, reformatted text), always run a dedup pass on the combined output....
- [soft] **supabase-env-guard**: When creating `@supabase/ssr` middleware in Next.js projects, always add an early-return guard for missing `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE...
- [soft] **supabase-migration-ghost-apply**: When debugging "table not found" errors on a Supabase-backed app, ALWAYS verify the table actually exists via REST API (`curl .../rest/v1/{table}?select=id&l...
- [soft] **supabase-status-constants**: When filtering by subscription status (or any enum-like status field) in Supabase/PostgREST queries, always use the canonical constant array (e.g. `ACTIVE_SU...
- [soft] **swarm-post-execution-review**: After swarm/parallel execution of 5+ stories, ALWAYS:
- [soft] **tailwind-v4-theme-tokens**: In Tailwind v4 projects using `@theme inline` blocks, prefer theme token utilities (`bg-{company}-plum`, `text-{company}-orange`) over arbitrary values (`bg-...
- [soft] **vercel-route53-domain-setup**: When adding a custom domain to a Vercel project where DNS is managed by AWS Route 53:
- [soft] **verify-routes-after-parallel-execution**: After parallel story execution completes, grep nav components for route hrefs and verify each has a corresponding `page.tsx`. Parallel agents add nav links i...
- [soft] **verify-vercel-project-exists**: Before running `vercel domains add` or assuming a Vercel project is live, verify it actually exists on the team with `vercel project ls --scope {team}`. The ...
- [soft] **worker-personality**: When creating or updating worker instructions, apply the negative experience memory pattern
- [soft] **worktree-launch-config**: When using git worktrees for project execution:

